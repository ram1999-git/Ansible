- name: Installing Packages
  hosts: localhost
  become: yes
  tasks:
    - name: Install Packages
      ansible.builtin.package:
        name: "{{ item }}"
        state: latest
      loop:
        - nginx
        - mysql
        - postfix


