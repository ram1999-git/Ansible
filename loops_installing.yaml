- name: Installing Packages
  hosts: localhost
  become: yes
  tasks:
    - name: Installing the given Packages
      ansible.builtin.debug:
        name: "{{ item }}"
        state: latest
      loop:
        - nginx
        - mysql-client
        - putfix