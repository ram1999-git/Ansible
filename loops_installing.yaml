- name: Installing Packages
  hosts: localhost
  become: yes
  tasks:
    - name: Install Packages
      ansible.builtin.debug:
        name: "{{ item }}"
        state: present
      loop:
        - nginx
        - mysql-client
        - putfix

