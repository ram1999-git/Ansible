- name: variables
  hosts: localhost
  vars:
    course_name: "DevOps"
    instructor: "Shiva"
    duration: "10hrs"
  tasks:
    - name: Print information
      vars:
       course_name: "Terraform"
       instructor: "Michel"
       duration: "19hrs"
      ansible.builtin.debug:
       msg: "Hi we are learning {{course_name}} from {{instructor}} Duration is {{duration}}"